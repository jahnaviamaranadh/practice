const value="234";
const number=value.length;
if(value[number-1]==="4")
{
    console.log("the given number have 4 at it's unit's place......... ");
}
else
{
    console.log("the given number don't have 4 at it's unit's place......... ");
}